import pandas as pd
import texthero as hero

def featurize_data(df: pd.DataFrame, dname = '20newsgroups', feat_type == 'tfidf'):
    if dname == '20newsgroups':
        if feat_type == 'tfidf':
            


